#dev dependencies
nodemon- constantly watch the server so need to refresh it everytime we do changes
concurrently - backendserver and reactserve with one single command



#regular dependencies
express -> main web framework for backends
express-validator -> If we post request to api if field needs to be there, and aren't will through an error, we
bcryptjs -> for password encryption. you dont need to store plain password in the database
config -> for global variables 
gravatar -> for profile avatars 
jsonwebtoken -> JWT to pass along the token for validation
mongoose -> layer thats seat top on the database 
request -> small module that allow us to make http request from another api 
1